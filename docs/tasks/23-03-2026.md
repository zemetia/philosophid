1. [FRONTEND] fix wizard register, every time i type the i lose input's focus from input (so it's like type 1 char then click again the input and so on) i want to check the root cause and fix this
2. [FRONTEND] in register wizard please remove "age" because we already have the birthday field
3. [FRONTEND] when i click "COMPLETE REGISTRATION" button in register wizard, i get
   http://localhost:3000/api/auth/sync
   Request Method
   POST
   Status Code
   401 Unauthorized
   Remote Address
   127.0.0.1:3000
   error 401, please make sure after logged in with manual and firebase, store JWT token of user_id + email in local storage as bearer token for x time (i can edit it so make a variable for it in .env or something)
