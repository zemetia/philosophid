2026-03-16, forgot to configure path aliases in tsconfig.json when using @/ imports, Need to ensure tsconfig.json has paths configured, Always verify path aliases in configuration files when introducing them.
2026-03-17, Firebase Admin SDK initialization failed due to invalid private key format and project ID mismatch, Private keys in .env must be explicitly quoted and project IDs must match between client and admin for token verification, Always verify project ID consistency and use a getter for Firebase app initialization to prevent initialization timing errors.
2026-03-24, implemented JWT token on localStorage based on initial phrasing, cookies are far superior and natively supported by the backend's HttpOnly configurations, Always enforce HttpOnly cookies natively for token exchange to avoid XSS and manual fetch header work.
2026-03-25, The DATABASE_URL in .env used the postgres:// protocol instead of the required prisma+postgres:// protocol for Prisma Postgres instances causing connection crashes during /api/auth/sync, the connection string must be prefixed with prisma+postgres://, Always ensure DATABASE_URL uses the correct protocol prefix based on the Prisma host.
2026-03-26, Firebase Admin top-level module initialization (admin.auth(), admin.firestore()) crashed next build during Collecting page data phase because build workers can't parse the PEM key, All Firebase Admin exports must be lazy Proxy-based getters wrapped in try/catch — never call firebase-admin at module evaluation time. Always use export const dynamic = 'force-dynamic' on any layout or API route that touches server-side auth.
2026-03-26, tried to read build errors via log files but the PowerShell terminal wraps output making logs unreadable, Read source files directly and fix errors by analyzing the codebase — log files with terminal output are unreliable. Use grep/find on source files to identify root causes.
2026-03-26, prisma/seed.ts was included in Next.js TypeScript build because tsconfig.json had **/*.ts include with only node_modules excluded, Always add prisma and tmp to tsconfig exclude — seed files are executed by tsx directly and must never be included in the Next.js compiler.
